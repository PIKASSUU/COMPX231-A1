multiple machines (producers) and a small number of printers (consumers) share a bounded print queue. 
When the queue is full, a new print request overwrites the request at the tail of the queue, causing data loss. 
